{
	"name": "obsidian-claude-notes",
	"version": "1.0.5",
	"description": "Obsidian plugin: sidebar, generate-note command, and live refresh for notes written by the Claude Code obsidian-notes CLI.",
	"main": "main.js",
	"type": "module",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"version": "node version-bump.mjs && git add manifest.json versions.json"
	},
	"keywords": ["obsidian", "obsidian-plugin", "claude-code"],
	"author": "Jefferson Goncalves",
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^26.4.0",
		"esbuild": "0.28.2",
		"obsidian": "latest",
		"typescript": "^5.9.3"
	}
}
